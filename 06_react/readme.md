how to update in dom (react)?
To update React DOM, you can use the following command in your terminal:

```bash
npm install react-dom@latest
```

# webrequest


 # page load state 

 page is about to load -> loading -> loaded -> interactive -> complete -> page is removed from DOM